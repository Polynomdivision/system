---
- name: <compton> Install compton
  pacman:
    name: compton
  become: yes

- name: <compton> Install the compton configuration
  file:
    state: link
    src: '{{ role_path }}/files/compton/compton.conf'
    dest: '{{ user_home }}/.compton.conf'
