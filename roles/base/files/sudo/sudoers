##
## Defaults
##
# Allow my docker services to be executed
Defaults env_keep += "USERID GROUPID"

##
## User privilege specification
##
root ALL=(ALL) ALL

# Allow members of sudo to execute any command
%sudo	ALL=(ALL) ALL

# Allow users in the dockerservuser group to use the following docker services without entering a password
%dockerservuser ALL=(ALL) NOPASSWD: /usr/local/bin/nvim, /usr/local/bin/docker_intellij, /usr/local/bin/docker_vscode

## Read drop-in files from /etc/sudoers.d
## (the '#' here does not indicate a comment)
#includedir /etc/sudoers.d