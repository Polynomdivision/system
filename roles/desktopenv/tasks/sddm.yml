---
- name: Installing SDDM
  pacman:
    name: sddm

- name: Configuring SDDM
  copy:
    src: '{{ role_path }}/files/sddm/sddm.conf'
    dest: /etc/sddm.conf
    mode: 0644

- name: Enabling SDDM
  systemd:
    enabled: yes
    name: sddm
