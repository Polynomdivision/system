---
- name: <user> Create a sudo group
  group:
    state: present
    name: sudo

- name: <user> Create a user account
  user:
    groups: sudo,dockerservuser
    createhome: yes
    state: present
    name: '{{ user }}'
    password: '{{ password_hash }}'
